<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <link rel="stylesheet" href="/css/master.css">     // to import a style from internet or local
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
<div class="container ">
  بنحط جواه كل الجدول اللي تحت
</div>


    <table class="table">
    <th></th>خانات الجدول colum
    <tr>// row

      <?php foreach ($variable as $key => $value):
        echo"<td>"".$contacts['id']."</td>";
        echo"<td>";
        echo"<td>";?>

     ?>
**implode('')
**explode('');//put more than one entity conected to 1-many relationship //لو user عندو اكتر من نمرة تلفيون مثلا
  </body>
</html>
//bootstrap //libraries used to import styles, forms-tables -buttons
-----------------------------------------------------------------------------------------------------------------------------------------
26/10/2018
  //add new contact to database
  $sql ="insert into tablename
  ('id','name')value('{$id}','{$name}');";
mysqli_insert_id($conn) //to return last id entered to db  لما يكون جزلين مرتبطين ببعض userid و phone number
          --------------------------------------------------------------------------------------------
placeholder -- // to put example in a field
if ($_server['Request_Method']=='post') //to check if the method is post

**handle errors
if(strlen(fname<5)) $errors[]="firsname must br ";
  //collect all errors in an array any print it
  redirect url -parameters
